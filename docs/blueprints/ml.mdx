---
id: blueprints-ml
slug: /blueprints/ml
title: Machine learning
sidebar_label: Machine learning
sidebar_position: 2
description: You do not configure training infrastructure. You draw it — and two moments stay deliberately human.
---

<Hero eyebrow="Blueprint" title="Draw the loop, not the plumbing">
An experiment is where runs are logged. A registered model is where trained versions live.
A job trains or applies one. Buckets hold data, labels and weights. The links between them
generate every address, name, credential, GPU placement and network rule.
</Hero>

## The shape

<Figure caption="The ML loop as a graph. A signed decision starts the training job; the trainer reads data and logs to an experiment; runs become versions of a registered model; serving loads whichever version holds the champion alias.">
<svg viewBox="0 0 900 360" role="img" aria-label="The machine-learning loop as a graph: a signed decision lets a GPU training job start, the trainer reads data and logs its run to an experiment, runs become versions of a registered model, and serving loads whichever version holds the champion alias — moved by a person, with a record.">
  <defs>
    <marker id="mlLoopAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
    <marker id="mlLoopABlue" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-blue)" /></marker>
    <marker id="mlLoopAMint" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-mint)" /></marker>
  </defs>
  <path d="M88 262 L250 262" fill="none" stroke="var(--stk-amber)" strokeWidth="1.5" markerEnd="url(#mlLoopAAmber)" />
  <path d="M262 250 L262 118" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#mlLoopABlue)" />
  <path d="M250 104 L102 104" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#mlLoopABlue)" />
  <path d="M274 104 L488 104" fill="none" stroke="var(--stk-mint)" strokeWidth="1.5" markerEnd="url(#mlLoopAMint)" />
  <path d="M500 116 L500 248" fill="none" stroke="var(--stk-mint)" strokeWidth="1.5" markerEnd="url(#mlLoopAMint)" />
  <path d="M514 262 L778 262" fill="none" stroke="var(--stk-blue)" strokeWidth="1.5" markerEnd="url(#mlLoopABlue)" />
  <path d="M640 305 L640 272" fill="none" stroke="var(--stk-amber)" strokeWidth="1.3" markerEnd="url(#mlLoopAAmber)" />
  <circle cx="90" cy="104" r="9" fill="var(--stk-blue)" />
  <circle cx="262" cy="104" r="16" fill="var(--stk-blue)" fillOpacity="0.16" />
  <circle cx="262" cy="104" r="10" fill="var(--stk-blue)" />
  <circle cx="262" cy="262" r="10" fill="var(--stk-blue)" />
  <path d="M72 248 L86 262 L72 276 L58 262 Z" fill="var(--stk-amber)" fillOpacity="0.2" stroke="var(--stk-amber)" strokeWidth="1.6" />
  <circle cx="500" cy="104" r="10" fill="var(--stk-mint)" />
  <circle cx="500" cy="262" r="18" fill="var(--stk-mint)" fillOpacity="0.16" />
  <circle cx="500" cy="262" r="12" fill="var(--stk-mint)" />
  <circle cx="790" cy="262" r="16" fill="var(--stk-blue)" fillOpacity="0.16" />
  <circle cx="790" cy="262" r="10" fill="var(--stk-blue)" />
  <path d="M640 306 L652 318 L640 330 L628 318 Z" fill="var(--stk-amber)" fillOpacity="0.2" stroke="var(--stk-amber)" strokeWidth="1.6" />
  <text x="90" y="128" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">buckets</text>
  <text x="90" y="141" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">DATA · LABELS · WEIGHTS</text>
  <text x="262" y="72" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">BUILT FROM YOUR REPO</text>
  <text x="262" y="85" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">your trainer</text>
  <text x="262" y="287" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">training job</text>
  <text x="262" y="300" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">GPU · TIME LIMIT</text>
  <text x="72" y="294" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">decision</text>
  <text x="72" y="307" fontSize="9.7" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">A PERSON SIGNS</text>
  <text x="500" y="72" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">EVERY RUN LANDS HERE</text>
  <text x="500" y="85" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">experiment</text>
  <text x="500" y="289" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">registered model</text>
  <text x="500" y="302" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">V1 · V2 · V3</text>
  <text x="790" y="287" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">serving · apply</text>
  <text x="790" y="300" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">LOADS @CHAMPION</text>
  <rect x="110.98" y="236.9" width="118.04" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="170" y="249.672" fontSize="10.2" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">starts only after</text>
  <rect x="230.52" y="174.9" width="62.96" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="262" y="187.672" fontSize="10.2" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">launches</text>
  <rect x="153.7" y="78.9" width="44.6" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="176" y="91.672" fontSize="10.2" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">reads</text>
  <rect x="336.28" y="78.9" width="87.44" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="380" y="91.672" fontSize="10.2" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">logs the run</text>
  <rect x="474.43" y="161.15" width="171.14" height="17.7" rx="4" fill="var(--stk-panel-2)" />
  <text x="560" y="173.492" fontSize="9.7" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">experiment-registered_model</text>
  <text x="560" y="190" fontSize="10.2" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">runs become versions</text>
  <rect x="562.62" y="236.9" width="154.76" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="640" y="249.672" fontSize="10.2" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">models:/&lt;name&gt;@champion</text>
  <text x="660" y="322" fontSize="11" fill="var(--stk-amber)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">promote = move the alias · a person signs</text>
</svg>
</Figure>

## What each piece is

| Piece | Role |
|---|---|
| **Experiment tracking and model registry** | Every run, every metric, every artifact, every version |
| **A relational database** | The registry's backend store, and pipeline metadata |
| **Object storage buckets** | Artifacts, curated data, labels, weights, run logs |
| **An orchestrator** | Runs training and apply jobs on demand, on GPU when asked |
| **Your trainer image** | Your own code, built by the platform from your repository |
| **A gateway** | The tracking interface's address, and a login in front of it when you want one |
| **Metrics** | Scrapes the tracking server's own health — not the metrics your runs log |
| **A decision record** | The signature a job reads before it starts, and that a promotion follows |

Without a database and a bucket, the registry falls back to local storage. That fallback is
for development only, and the platform says so rather than letting you discover it.

## The two sub-components that carry the loop

| Sub-component | Side | Who links to it | What the link generates |
|---|---|---|---|
| **Experiment** | Write | Trainers — a job, an asset, or your own code component | The tracking address and the experiment name, so the run lands in the right place |
| **Registered model** | Read | Consumers — an apply job or a serving component | The model address, its name and its alias |

A sub-component-to-sub-component link binds them: *runs logged here become versions there*.
A trainer linked only to the experiment still learns which model to register to, because
the binding is read from the experiment's own record.

Both are created for you when the registry deploys, idempotently, with their tags and
descriptions.

## Promotion is moving one alias

Consumers load the model by **alias**, not by version number. Promotion is a person moving
the alias to another version.

No rebuild. No redeploy. The next run loads the new champion, and rolling back is moving
the alias back.

## Two deliberate human moments

<Cards cols={2}>
  <Card kicker="Before" title="Starting a run">
    A training run can cost real money. A job can be made to refuse to start until a
    decision record carries a signature.
  </Card>
  <Card kicker="After" title="Promoting a version">
    A promotion changes what your users get. It can be made to follow a signed decision
    rather than a chat message.
  </Card>
</Cards>

Everything between those two moments is automatic. Both signatures are records, and an AI
may propose them but never sign them.

→ [Reviews and approvals](/docs/compliance/reviews)

## GPU, queueing and cost

GPU placement comes from the job's attributes — the node selection, the sharing mode, the
memory fraction per model. Heavy periodic work can be admitted through a queue rather than
competing with production workloads, with priority expressed as an attribute.

Because all of it is on the graph, the expensive parts of an ML platform are visible in a
diff and roll back with everything else.

→ [Lifecycle and scale](/docs/day2/lifecycle)

---
id: compliance-frameworks
slug: /compliance/frameworks
title: PCI DSS and ISO 27001
sidebar_label: Frameworks
sidebar_position: 2
description: How the compliance position is computed from the topology and the cluster, and what the generated audit report contains.
---

<Hero eyebrow="Frameworks" title="From topology to position">
The framework scanners start from what your stack actually contains, decide which controls
are even applicable, then go and measure them.
</Hero>

<Figure caption="From topology to report. What the stack can do decides which controls apply; each is measured in the cluster; the report puts the attestation, the evidence and the human position side by side.">
<svg viewBox="0 0 900 300" role="img" aria-label="The framework scan starts from the graph: what the stack can do decides which controls apply and how they can be met; each control is measured in the cluster; the report carries the attestation, the evidence and the human position side by side.">
  <defs>
    <marker id="frameworksAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
    <marker id="frameworksABlue" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-blue)" /></marker>
    <marker id="frameworksAViolet" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-violet)" /></marker>
  </defs>
  <path d="M60 150 L108 150" fill="none" stroke="var(--stk-amber)" strokeWidth="1.4" markerEnd="url(#frameworksAAmber)" />
  <path d="M324 144 L376 144" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#frameworksABlue)" />
  <path d="M578 144 L638 144" fill="none" stroke="var(--stk-violet)" strokeWidth="1.4" markerEnd="url(#frameworksAViolet)" />
  <rect x="110" y="62" width="214" height="170" rx="12" fill="var(--stk-panel)" />
  <rect x="110" y="62" width="214" height="170" rx="12" fill="var(--stk-blue)" fillOpacity="0.045" stroke="var(--stk-blue)" strokeOpacity="0.5" strokeWidth="1.1" />
  <rect x="378" y="86" width="200" height="116" rx="12" fill="var(--stk-panel)" />
  <rect x="378" y="86" width="200" height="116" rx="12" fill="var(--stk-violet)" fillOpacity="0.045" stroke="var(--stk-violet)" strokeOpacity="0.5" strokeWidth="1.1" />
  <rect x="640" y="56" width="236" height="184" rx="12" fill="var(--stk-panel)" />
  <rect x="640" y="56" width="236" height="184" rx="12" fill="var(--stk-mint)" fillOpacity="0.11" stroke="var(--stk-mint)" strokeOpacity="1" strokeWidth="1.6" />
  <circle cx="46" cy="150" r="17" fill="var(--stk-amber)" fillOpacity="0.16" />
  <circle cx="46" cy="150" r="11" fill="var(--stk-amber)" />
  <text x="46" y="176" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">your graph</text>
  <text x="124" y="82" fontSize="9.7" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" fontWeight="600" letterSpacing="1.1">CAPABILITY PICTURE</text>
  <text x="124" y="99" fontSize="13.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">what this stack can do</text>
  <text x="124" y="116" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">policy engine    ✓</text>
  <text x="124" y="132" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">secret store     ✓</text>
  <text x="124" y="148" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">certificates     ✓</text>
  <text x="124" y="164" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">backup           ✓</text>
  <text x="124" y="180" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">SIEM             ✗</text>
  <text x="392" y="106" fontSize="9.7" fill="var(--stk-violet)" fontFamily="Fira Code, ui-monospace, monospace" fontWeight="600" letterSpacing="1.1">APPLICABLE CONTROLS</text>
  <text x="392" y="123" fontSize="13.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">PCI DSS · ISO 27001</text>
  <text x="392" y="140" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">PCI DSS v4.0 · 250</text>
  <text x="392" y="155" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">ISO 27001:2022 · 93</text>
  <text x="392" y="170" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">no SIEM = a named gap</text>
  <rect x="321.58" y="118.9" width="56.84" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="350" y="131.672" fontSize="10.2" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">decides</text>
  <text x="654" y="76" fontSize="9.7" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" fontWeight="600" letterSpacing="1.1">THE REPORT</text>
  <text x="654" y="93" fontSize="13.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">an audit document</text>
  <text x="654" y="110" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">attestation: what was judged</text>
  <text x="654" y="126" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">summary · coverage</text>
  <text x="654" y="142" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">evidence for every control</text>
  <text x="654" y="158" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">the human position,</text>
  <text x="654" y="174" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">side by side</text>
  <rect x="576.52" y="118.9" width="62.96" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="608" y="131.672" fontSize="10.2" fill="var(--stk-violet)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">measured</text>
</svg>
</Figure>

## Capability first, then controls

Before evaluating anything, the scanner builds a **capability picture** from the graph:
does this stack have a policy engine, a certificate authority, a secret store, a SIEM, a
backup mechanism, an identity provider, a gateway, runtime detection.

That picture decides which controls apply and how they can possibly be satisfied. A
requirement about centralised log retention is evaluated differently in a stack that has a
log pipeline than in one that does not — and in the second case the answer is a named gap,
not a silent pass.

## The three modes

| Mode | What it does |
|---|---|
| **Fast** | Collects cluster evidence quickly: workloads, resource types, health, counts |
| **Deep** | Adds hardening scans, admission dry-runs and deeper analysis |
| **Report** | Everything in deep, plus a rendered audit document |

## What the report contains

The generated report is a document an auditor can read without a walkthrough:

<Steps>
  <Step title="An attestation table">
    The result hash, the hash and commit of the declared system it judged, the cluster
    identity and the agent build. The report names the state it was true of.
  </Step>
  <Step title="An executive summary">
    Counts by status, coverage per requirement, and the review coverage.
  </Step>
  <Step title="Per-control findings">
    The automated status, the finding, and the evidence — command, output, result.
  </Step>
  <Step title="The human position">
    Under each control, the recorded review: who signed it, why, and when it expires.
  </Step>
</Steps>

The machine finding and the human position are always shown **side by side**. The automated
status is never replaced by a sign-off.

## From a gap to a plan

A gap is only useful if it says what to do. The framework tools answer three questions in
sequence:

| Question | What comes back |
|---|---|
| Where do I stand? | Overview: counts by status, per-requirement coverage, and what the stack is capable of |
| What is missing? | Gaps with the evidence behind each one |
| What fixes it? | The change to the graph — usually an attribute, sometimes a component or a link |
| What would this component change? | Per-component impact: which controls a given technology moves if you add, remove or reconfigure it |

Because the remediation is a graph change, "we fixed it" is verifiable by re-running the
same scan and comparing the two runs — and the comparison is classified as an improvement
explained by a declared change, not as unexplained movement.

## Continuous, not annual

A compliance position computed once a year describes a system that no longer exists.

Framework scans participate in the same schedule, delta and attestation machinery as every
other scan: they can run continuously, every run is chained to the previous one, a
regression is confirmed by a re-run before anyone is alerted, and a labelled run becomes
the baseline that drift is measured against.

→ [Evidence and drift](/docs/day2/evidence)

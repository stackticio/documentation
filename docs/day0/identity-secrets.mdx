---
id: day0-identity-secrets
slug: /day0/identity-secrets
title: Identity and secrets
sidebar_label: Identity and secrets
sidebar_position: 4
description: Users and groups as declared state, single sign-on generated from links, and two ways to hold a secret.
---

<Hero eyebrow="Access" title="People and credentials are part of the graph">
A user is not something you create in an application. It is an attribute of the system,
and it propagates to every application the graph says it should reach.
</Hero>

<Figure caption="People are declared once, as lines on the identity client. The build syncs them to the identity provider and single sign-on carries them everywhere. An account created inside an app is drift the graph never sees.">
<svg viewBox="0 0 900 344" role="img" aria-label="People are declared once, as lines on the identity client sub-component. A build runs the sync jobs, the identity provider holds the accounts, and single sign-on carries them into every linked application. An account created inside an application is drift the graph never sees.">
  <defs>
    <marker id="identityAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
    <marker id="identityAMint" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-mint)" /></marker>
  </defs>
  <path d="M286 115 L348 115" fill="none" stroke="var(--stk-amber)" strokeWidth="1.5" markerEnd="url(#identityAAmber)" />
  <path d="M372 115 L486 115" fill="none" stroke="var(--stk-amber)" strokeWidth="1.5" markerEnd="url(#identityAAmber)" />
  <path d="M514 115 C627 115 627 64 740 64" fill="none" stroke="var(--stk-mint)" strokeWidth="1.3" markerEnd="url(#identityAMint)" />
  <path d="M514 115 C627 115 627 118 740 118" fill="none" stroke="var(--stk-mint)" strokeWidth="1.3" markerEnd="url(#identityAMint)" />
  <path d="M514 115 C627 115 627 172 740 172" fill="none" stroke="var(--stk-mint)" strokeWidth="1.3" markerEnd="url(#identityAMint)" />
  <path d="M514 115 C627 115 627 226 740 226" fill="none" stroke="var(--stk-mint)" strokeWidth="1.3" markerEnd="url(#identityAMint)" />
  <path d="M752 284 L752 236" fill="none" stroke="var(--stk-red)" strokeWidth="1.3" strokeDasharray="4 4" />
  <rect x="24" y="60" width="260" height="110" rx="12" fill="var(--stk-panel)" />
  <rect x="24" y="60" width="260" height="110" rx="12" fill="var(--stk-amber)" fillOpacity="0.11" stroke="var(--stk-amber)" strokeOpacity="1" strokeWidth="1.6" />
  <circle cx="360" cy="115" r="9" fill="var(--stk-amber)" />
  <circle cx="500" cy="115" r="18" fill="var(--stk-blue)" fillOpacity="0.16" />
  <circle cx="500" cy="115" r="12" fill="var(--stk-blue)" />
  <circle cx="752" cy="64" r="7" fill="var(--stk-mint)" />
  <circle cx="752" cy="118" r="7" fill="var(--stk-mint)" />
  <circle cx="752" cy="172" r="7" fill="var(--stk-mint)" />
  <circle cx="752" cy="226" r="7" fill="var(--stk-mint)" />
  <circle cx="752" cy="292" r="7" fill="var(--stk-red)" />
  <text x="38" y="80" fontSize="9.7" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" fontWeight="600" letterSpacing="1.1">DECLARED · CLIENT SUB-COMPONENT</text>
  <text x="38" y="97" fontSize="13.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">users</text>
  <text x="38" y="114" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">username:email:password:first:last</text>
  <text x="38" y="128" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">one line per person</text>
  <text x="38" y="142" fontSize="10.5" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace">+ groups on the provider</text>
  <text x="360" y="139" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">sync jobs</text>
  <text x="360" y="152" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">ON BUILD + DEPLOY</text>
  <text x="500" y="142" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">identity provider</text>
  <text x="500" y="155" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">ACCOUNTS · GROUPS</text>
  <text x="767" y="68" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">files</text>
  <text x="767" y="122" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">mail</text>
  <text x="767" y="176" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">wiki</text>
  <text x="767" y="230" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" fontWeight="600">your app</text>
  <rect x="590.16" y="80.9" width="99.68" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="590.16" y="80.9" width="99.68" height="18.2" rx="9.1" fill="var(--stk-mint)" fillOpacity="0.14" stroke="var(--stk-mint)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="640" y="93.672" fontSize="10.2" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">single sign-on</text>
  <text x="738" y="296" fontSize="11.8" fill="var(--stk-red)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="end" fontWeight="600">account made inside the app</text>
  <rect x="528.96" y="314.9" width="222.08" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="528.96" y="314.9" width="222.08" height="18.2" rx="9.1" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="640" y="327.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">drift · the sync jobs never see it</text>
</svg>
</Figure>

## Users and groups are declared

System users live as lines on the identity provider's client sub-component. Groups live on
the identity provider itself. Both are ordinary attributes, so both are versioned with the
stack, duplicated with it into another environment, and rolled back with it.

| Declared as | Shape |
|---|---|
| A user | `username : email : password : first name : last name` plus optional flags such as multi-factor |
| A group | `name : email : members` |

On the next build and deploy, synchronisation jobs derive the actual accounts from those
lines, and single sign-on carries the identity into every application that has an identity
link.

The username is the durable key. Renaming a person changes their display name; it does not
create a second account.

:::danger Creating users inside an application is drift
An account created directly through an application's own admin interface is invisible to
the graph, skipped by the synchronisation jobs, and silently diverging from the declared
system. It will appear to work, and then quietly stop matching everything else.

Onboarding and offboarding go through the platform — which is exactly what the MCP
`user_lifecycle` tool does, including telling you what will propagate where.
:::

## Single sign-on is a link

An application does not get a login because someone configured one. It gets a login
because there is a link between it and the identity provider — and that link generates the
OIDC client, the client secret, the redirect URIs and the application-side configuration
together.

Add the link, and the application is behind a login. Remove it, and it is not. There is no
third state where the client exists but the application does not know about it.

## Two ways to hold a secret

<Cards cols={2}>
  <Card kicker="Default" title="Encrypted in the repository">
    Credentials are generated per instance and encrypted at rest in your stack repository.
    Decryption is local and offline, with a key you hold. Nothing external is required, and
    the stack installs in an air-gapped environment unchanged.
  </Card>
  <Card kicker="Optional" title="Sourced from a secret store">
    Components can instead pull their secrets from an external secret store at runtime. The
    component declares that it wants this; the operator and the store are components of the
    stack like anything else.
  </Card>
</Cards>

Both are expressible on the same graph, and the choice is per component rather than
all-or-nothing.

:::caution A store token is a runtime credential
When secrets come from an external store, the store's access token is held as a design-time
attribute. Re-initialising the store therefore leaves the stack presenting a token that no
longer exists — and the failure is indirect: the store goes invalid, every secret stops
syncing, workloads cannot start, and the symptom you actually see is an application error
with no obvious link to secrets.

Two valid recoveries: refresh the attribute with the new token, or switch the affected
components back to generating their secrets locally.
:::

## Every instance gets its own credential

A catalog default is written for one instance. The moment a component owns a second
sub-component of the same kind — two buckets, three databases, four clients — each one
needs its own name, user and secret.

This is enforced at generation time. Two buckets sharing an access key do not produce a
subtly wrong stack; they produce a build that stops and tells you which value has to
differ.

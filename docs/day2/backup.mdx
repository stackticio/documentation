---
id: day2-backup
slug: /day2/backup
title: Backup and recovery
sidebar_label: Backup and recovery
sidebar_position: 5
description: What is protected, what can be proven to hold data, which restore points are usable, and a restore that cannot be triggered by accident.
---

<Hero eyebrow="Recover" title="A backup that reports success and holds nothing">
That is the failure this exists to catch. Backup tooling routinely reaches a completed
state whether it copied gigabytes or nothing at all — and nobody finds out until the day it
matters.
</Hero>

## Discovered from the graph

Protection is not configured twice. A link drawn between a database and a storage target is
an **intent**; the agent reports whether the cluster actually honours it. Because the
mechanisms are discovered from the metadata structure, this works on any stack without
per-stack configuration.

<Figure caption="How a backup verdict is reached, starting from the link the diagram declares. The case that matters most is a run that reports success and holds no data.">
<svg viewBox="0 0 900 350" role="img" aria-label="How a backup verdict is reached: from the link the diagram declares, the agent checks that a mechanism exists, that it has run, and that bytes were captured — and a run that completed with no data is reported as the dangerous case it is.">
  <defs>
    <marker id="backupTreeAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
    <marker id="backupTreeAMint" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-mint)" /></marker>
    <marker id="backupTreeARed" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-red)" /></marker>
  </defs>
  <path d="M54 90 L166 90" fill="none" stroke="var(--stk-amber)" strokeWidth="2" />
  <path d="M110 104 C110 190 200 190 274 190" fill="none" stroke="var(--stk-amber)" strokeWidth="1.4" markerEnd="url(#backupTreeAAmber)" />
  <path d="M307 190 L463 190" fill="none" stroke="var(--stk-mint)" strokeWidth="1.4" markerEnd="url(#backupTreeAMint)" />
  <path d="M497 190 L653 190" fill="none" stroke="var(--stk-mint)" strokeWidth="1.4" markerEnd="url(#backupTreeAMint)" />
  <path d="M687 190 L776 190" fill="none" stroke="var(--stk-mint)" strokeWidth="1.5" markerEnd="url(#backupTreeAMint)" />
  <path d="M290 207 L290 268" fill="none" stroke="var(--stk-red)" strokeWidth="1.3" markerEnd="url(#backupTreeARed)" />
  <path d="M480 207 L480 268" fill="none" stroke="var(--stk-red)" strokeWidth="1.3" markerEnd="url(#backupTreeARed)" />
  <path d="M670 207 L670 268" fill="none" stroke="var(--stk-red)" strokeWidth="1.5" markerEnd="url(#backupTreeARed)" />
  <circle cx="44" cy="90" r="8" fill="var(--stk-blue)" />
  <circle cx="176" cy="90" r="8" fill="var(--stk-blue)" />
  <path d="M290 175 L305 190 L290 205 L275 190 Z" fill="var(--stk-panel)" stroke="var(--stk-steel)" strokeWidth="1.5" />
  <path d="M480 175 L495 190 L480 205 L465 190 Z" fill="var(--stk-panel)" stroke="var(--stk-steel)" strokeWidth="1.5" />
  <path d="M670 175 L685 190 L670 205 L655 190 Z" fill="var(--stk-panel)" stroke="var(--stk-steel)" strokeWidth="1.5" />
  <text x="24" y="38" fontSize="9.7" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" fontWeight="600" letterSpacing="1.1">THE DIAGRAM'S INTENT</text>
  <text x="44" y="113" fontSize="11.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">database</text>
  <text x="176" y="113" fontSize="11.5" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">bucket</text>
  <rect x="70.99" y="61.15" width="78.02" height="17.7" rx="8.85" fill="var(--stk-panel-2)" />
  <rect x="70.99" y="61.15" width="78.02" height="17.7" rx="8.85" fill="var(--stk-amber)" fillOpacity="0.14" stroke="var(--stk-amber)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="110" y="73.492" fontSize="9.7" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">backup link</text>
  <text x="290" y="150" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">mechanism</text>
  <text x="290" y="164" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">present?</text>
  <rect x="368.82" y="166.9" width="32.36" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="385" y="179.672" fontSize="10.2" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">yes</text>
  <text x="480" y="150" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">has it</text>
  <text x="480" y="164" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">run?</text>
  <rect x="558.82" y="166.9" width="32.36" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="575" y="179.672" fontSize="10.2" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">yes</text>
  <text x="670" y="150" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">bytes captured</text>
  <text x="670" y="164" fontSize="11.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">and verified?</text>
  <rect x="803.3" y="180.5" width="73.4" height="19" rx="9.5" fill="var(--stk-panel-2)" />
  <rect x="803.3" y="180.5" width="73.4" height="19" rx="9.5" fill="var(--stk-mint)" fillOpacity="0.14" stroke="var(--stk-mint)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="840" y="193.96" fontSize="11" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">PROTECTED</text>
  <rect x="302.88" y="226.9" width="26.24" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="316" y="239.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">no</text>
  <rect x="243.22" y="278.9" width="93.56" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="243.22" y="278.9" width="93.56" height="18.2" rx="9.1" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="290" y="291.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">NOT_PROTECTED</text>
  <rect x="492.88" y="226.9" width="26.24" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="506" y="239.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">no</text>
  <rect x="445.46" y="278.9" width="69.08" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="445.46" y="278.9" width="69.08" height="18.2" rx="9.1" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="480" y="291.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">NEVER_RAN</text>
  <rect x="677.58" y="226.9" width="56.84" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="706" y="239.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">0 bytes</text>
  <rect x="606.9" y="278.5" width="126.2" height="19" rx="9.5" fill="var(--stk-panel-2)" />
  <rect x="606.9" y="278.5" width="126.2" height="19" rx="9.5" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="670" y="291.96" fontSize="11" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">COMPLETED_NO_DATA</text>
  <text x="670" y="320" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">reports success · holds nothing</text>
  <rect x="375.49" y="99.15" width="369.02" height="17.7" rx="4" fill="var(--stk-panel-2)" />
  <text x="560" y="111.492" fontSize="9.7" fill="var(--stk-steel)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">could not verify → INCONCLUSIVE · not the same as unprotected</text>
</svg>
</Figure>

## The verdict that matters

| Verdict | Meaning |
|---|---|
| `PROTECTED` | Bytes were captured and verified |
| `COMPLETED_NO_DATA` | **The dangerous one.** The backup reports success and holds zero volume data |
| `NEVER_RAN` | Configured, never executed |
| `NOT_PROTECTED` | The mechanism is missing or misconfigured |
| `INCONCLUSIVE` | Could not be verified — a permission denial or an absent resource type. **Not** the same as unprotected |

Only the deep scan mode can prove that data exists; the fast mode reports structure. A
score of `null` means the stack has no backup wiring at all — which is "nothing is
configured", not zero percent.

## Drift, both directions

| | What it is |
|---|---|
| `ORPHAN` | A resource on the cluster that nothing in the graph asks for. Kustomize does not prune, so these accumulate |
| `VOID` | A link that generates nothing — the diagram showing protection that does not exist |

## Restore points are a catalogue, not a list

A restore should be chosen *from* something. The agent produces a dated catalogue of every
point you could restore from, grouped by day, each one carrying what it costs to pick it:
when it was taken, how many bytes it actually holds, when it expires, whether a human has
certified it, and — when it cannot be used — the reason.

A point is **not selectable** when it partially failed, when it captured zero bytes, when
it has expired, or when its mechanism cannot target a specific artefact. The same rule the
scan enforces is applied at the moment of choosing: a backup that cannot be proven is never
offered as a restore source.

Where a mechanism has a limitation, it is stated on the point rather than hidden. Artefacts
are listed so you can see what exists, marked unusable with the reason.

## Recovery horizon is an attribute of the relationship

Schedule and retention live on the link between the data and its storage target, not in a
separate backup configuration. The cadence and the recovery horizon are therefore part of
the graph: versioned with it, duplicated into staging with it, visible in a diff.

Where a horizon was never set explicitly, the scan says so, so an inherited default is
visible as an inherited default.

## Day-2 control is opt-in and layered

Reading and rendering manifests always works. **Applying** anything requires every one of
the following:

<Steps>
  <Step title="API authentication">
    The agent's key, when one is set.
  </Step>
  <Step title="An explicit attribute on the component">
    Write operations are off by default. In-place restore needs a second attribute on top.
  </Step>
  <Step title="A rendered manifest">
    The exact YAML is produced and server-side dry-run first. A confirmation token embeds
    the resolved target, so the manifest step cannot be skipped.
  </Step>
  <Step title="An exact confirmation token">
    Naming the subject and, for a restore, where the data lands.
  </Step>
  <Step title="A typed human phrase — for restores only">
    Case-sensitive and exact. Enforced by the API, not only by the interface, so it cannot
    be pre-filled by a caller acting on someone's behalf. Taking a backup needs no phrase.
  </Step>
  <Step title="An admitted dry run">
    Before anything is applied for real.
  </Step>
</Steps>

Restores default to a **scratch namespace**; the live namespace is untouched unless you
have explicitly enabled in-place restore. Every attempt — including every refusal — is
recorded in the agent's own write audit trail.

## Certification and pinning

A restore point can be **certified** by a named person, and that certification is part of
its history. A point can also be **pinned**: a new backup taken with a long retention and a
label, which is the only honest way to keep a checkpoint when retention is time-based and
existing artefacts cannot be exempted from it.

## Continuously replicating mechanisms

Some mechanisms do not have runs — off-site replication, write-ahead archiving. They have
no schedule, no retention window and no per-run byte count; they are healthy when their
workload is ready. The interface reads this from the mechanism's own declared capabilities,
so these are shown correctly without a special case, and a new mechanism needs no interface
change.

---
id: day2-lifecycle
slug: /day2/lifecycle
title: Lifecycle and scale
sidebar_label: Lifecycle and scale
sidebar_position: 6
description: Upgrades and migrations from declarative recipes, autoscaling from real signals, restart-on-change, and workload queueing.
---

<Hero eyebrow="Change" title="The hard part of Day 2 is doing it in the right order">
Upgrading a component is rarely hard. Knowing what depends on it, what has to move first,
what proves it worked and how to get back is the hard part — and that is exactly what a
graph can answer.
</Hero>

<Figure caption="An upgrade is a recipe, not a script: check health first, migrate only between the right versions, apply as a graph change, verify with the component's own tests — and revert and rebuild if they fail.">
<svg viewBox="0 0 900 300" role="img" aria-label="Every upgrade follows a declarative recipe: preflight checks, migrations that apply only between the right versions, the change applied as a graph change and a build, and the component's own tests to verify — with a defined rollback when verification fails.">
  <defs>
    <marker id="recipeASteel" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-steel)" /></marker>
    <marker id="recipeARed" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-red)" /></marker>
  </defs>
  <path d="M83 130 L237 130" fill="none" stroke="var(--stk-steel)" strokeWidth="1.4" markerEnd="url(#recipeASteel)" />
  <path d="M263 130 L417 130" fill="none" stroke="var(--stk-steel)" strokeWidth="1.4" markerEnd="url(#recipeASteel)" />
  <path d="M443 130 L597 130" fill="none" stroke="var(--stk-steel)" strokeWidth="1.4" markerEnd="url(#recipeASteel)" />
  <path d="M623 130 L777 130" fill="none" stroke="var(--stk-steel)" strokeWidth="1.4" markerEnd="url(#recipeASteel)" />
  <path d="M70 176 L70 230" fill="none" stroke="var(--stk-red)" strokeWidth="1.3" markerEnd="url(#recipeARed)" />
  <path d="M610 176 C610 250 460 250 440 176" fill="none" stroke="var(--stk-red)" strokeWidth="1.3" strokeDasharray="4 4" markerEnd="url(#recipeARed)" />
  <circle cx="70" cy="130" r="8" fill="var(--stk-blue)" />
  <circle cx="250" cy="130" r="8" fill="var(--stk-blue)" />
  <circle cx="430" cy="130" r="16" fill="var(--stk-amber)" fillOpacity="0.16" />
  <circle cx="430" cy="130" r="10" fill="var(--stk-amber)" />
  <circle cx="610" cy="130" r="8" fill="var(--stk-mint)" />
  <circle cx="790" cy="130" r="16" fill="var(--stk-mint)" fillOpacity="0.16" />
  <circle cx="790" cy="130" r="10" fill="var(--stk-mint)" />
  <rect x="263.45" y="26.75" width="373.1" height="18.5" rx="9.25" fill="var(--stk-panel-2)" />
  <rect x="263.45" y="26.75" width="373.1" height="18.5" rx="9.25" fill="var(--stk-blue)" fillOpacity="0.14" stroke="var(--stk-blue)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="450" y="39.78" fontSize="10.5" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">a recipe is data, not code · new coverage = one new entry</text>
  <text x="70" y="153" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">preflight</text>
  <text x="70" y="166" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">HEALTHY FIRST?</text>
  <text x="250" y="153" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">migrations</text>
  <text x="250" y="166" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">ONLY WHERE NEEDED</text>
  <text x="430" y="155" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">apply</text>
  <text x="430" y="168" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">GRAPH CHANGE + BUILD</text>
  <text x="610" y="153" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">verify</text>
  <text x="610" y="166" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">THE COMPONENT'S TESTS</text>
  <text x="790" y="155" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">done</text>
  <text x="790" y="168" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">NEW VERSION RUNS</text>
  <rect x="7.5" y="241.15" width="305" height="17.7" rx="8.85" fill="var(--stk-panel-2)" />
  <rect x="7.5" y="241.15" width="305" height="17.7" rx="8.85" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="160" y="253.492" fontSize="9.7" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">unhealthy → stop · an upgrade would hide the cause</text>
  <rect x="477.34" y="253.15" width="165.32" height="17.7" rx="8.85" fill="var(--stk-panel-2)" />
  <rect x="477.34" y="253.15" width="165.32" height="17.7" rx="8.85" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="560" y="265.492" fontSize="9.7" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">fails → revert and rebuild</text>
</svg>
</Figure>

## Upgrades and migrations

Each component's upgrade procedure is a **recipe**: declarative data, not code. Adding
coverage for a component means adding an entry, never writing new logic.

| Part of a recipe | What it holds |
|---|---|
| **Version fields** | Which attributes actually carry the version |
| **Preflight** | Probes that must pass before anything is touched, each with the risk of proceeding anyway — starting with "is this component healthy right now", because upgrading something already broken hides the cause |
| **Migrations** | Conditional steps that apply only between specific versions, each with its reverse |
| **Apply** | Change the attribute on the graph, then build — never a hand-patched cluster |
| **Verify** | The component's own tests, run after |
| **Rollback** | Revert the manifests, rebuild |
| **Known gaps** | What the template does not yet handle, stated rather than discovered |

The lifecycle tool reports status and produces a plan: the current versions, the
dependents that will be affected, the preflight results, and the ordered steps. The AI
proposes it; you approve it; the platform performs it as an ordinary graph change.

## Database migrations

Schema migration is expressed as a **sub-component** of the database, so it is part of the
declared system and moves with it. Migrations run as an init container against the
component's own image, the workload surges by zero so two versions never run against one
schema, and a failed migration fails closed rather than starting an application against a
half-migrated database.

Backups stay with whatever already owns them — a migration step does not invent its own
backup mechanism next to the one that is already proven.

## Autoscaling from real signals

Autoscaling is driven by what the system actually reports, not only by CPU. Scale targets
are declared as sub-components of the autoscaler, each bound to a signal:

<Pills>
  <Pill>a metrics query</Pill>
  <Pill>a log query</Pill>
  <Pill>inference load on a served model</Pill>
  <Pill>queue depth</Pill>
</Pills>

Because the autoscaler is a component and its triggers are sub-components, scaling
behaviour is versioned with the stack and visible in a diff — not a cluster-side object
someone edited once.

## Restart on change

A generated secret or configuration value that changes must actually reach the running
pod. Two complementary mechanisms exist, and both are part of the graph:

<Cards cols={2}>
  <Card kicker="Generation-time" title="Content checksums">
    The build adds a checksum of exactly the secrets and configuration a workload
    references to its pod template. Change the value, regenerate, apply — the pod template
    changed, so the workload rolls. Nothing else does.
  </Card>
  <Card kicker="Runtime" title="A reload controller">
    A controller watches labelled secrets and configuration and restarts the workloads that
    consume them. Scoped by label, so it acts on what you opted in.
  </Card>
</Cards>

Without one of these, a changed secret sits correctly in the API server while the running
pod keeps the old value — which is the class of bug that produces "it works after I restart
it by hand".

## Queueing and priority

Batch and training workloads can be admitted through a queue rather than competing for
whatever the cluster has left. Jobs are labelled into a queue automatically, and priority is
expressed as an attribute on the workload that needs it.

This is what makes a shared cluster usable for both a production application and heavy
periodic work, without either one starving the other.

## Everything here is a graph change

The common thread: none of these are cluster-side operations you perform and then have to
remember. Each one is an attribute or a sub-component on the graph. It is versioned,
diffable, duplicable into another environment, rollback-able with the rest of the system,
and computable — which is why an AI can plan it safely and why a human can approve it in
one look.

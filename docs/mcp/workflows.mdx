---
id: mcp-workflows
slug: /mcp/workflows
title: Working with the model
sidebar_label: Working with the model
sidebar_position: 6
description: The four conversations that cover almost everything — design, change, diagnose, prove.
---

<Hero eyebrow="Practice" title="Four conversations">
Once the MCP is connected, almost everything you do with Stacktic is one of four
exchanges. Each one has a shape the server enforces, so the outcome does not depend on how
well you prompted.
</Hero>

## 1 · Design a stack from a specification

Paste your product or data specification into a client connected to the MCP. The model
does not start drawing immediately — it works outward from your application, in a fixed
order, and asks the smallest set of questions that change the result.

<Steps>
  <Step title="The application">
    What it does, what port it listens on, which language and framework. This decides the
    environment-variable convention the application will be handed.
  </Step>
  <Step title="What it needs">
    Database, object storage, cache, queue, search, scheduled jobs — one component per
    need, never two that do the same job.
  </Step>
  <Step title="Who reaches it">
    A public URL, a login, or internal only. This is what adds a gateway, an identity
    client, and the routes and redirect URIs that go with them.
  </Step>
  <Step title="How it runs">
    Environments, replicas or autoscaling, GPU, resource sizes.
  </Step>
  <Step title="What proves it works">
    Metrics, dashboards, logs, backups — last, and only the ones you will actually look at.
  </Step>
</Steps>

Every question arrives in one message with a sensible default, so "defaults are fine" is a
complete answer. Anything the catalog cannot express is reported as a **gap** — named
precisely, with the smallest change that would unblock it — rather than substituted with
something that merely compiles.

:::tip Your code needs a home
An application component does nothing without a repository. The model will ask which of
exactly two options you want: a new repository, or a subfolder in a repository the stack
already uses. The second is the cheaper default and needs no new credentials — each
application gets its own folder and its own container build, named after the component.
:::

## 2 · Change something safely

The dangerous part of an autonomous agent is not that it makes changes. It is that it
makes changes whose consequences nobody computed.

<Steps>
  <Step title="Read the current value">
    Never a change proposed against an assumed state.
  </Step>
  <Step title="Show it to you">
    The value as it is now, from a tool result.
  </Step>
  <Step title="State the blast radius">
    What this attribute touches, which components depend on it, and in what order things
    should move.
  </Step>
  <Step title="Ask">
    Options enumerated from the real catalog — never free text.
  </Step>
  <Step title="Write, build, deploy, verify">
    The change lands in the graph; the build regenerates everything that depends on it; the
    runtime is checked afterwards, not before.
  </Step>
</Steps>

<Figure caption="Every change follows the same path: read, show, compute the blast radius, ask — then write, build, deploy and prove it. If the proof fails, the previous version is one step away.">
<svg viewBox="0 0 900 300" role="img" aria-label="How the model changes a live system: read the current value, show it, compute the blast radius, ask, and only then write, build, deploy and verify — returning to the previous version if verification fails.">
  <defs>
    <marker id="safeChangeABlue" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-blue)" /></marker>
    <marker id="safeChangeAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
    <marker id="safeChangeAGit" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-git)" /></marker>
    <marker id="safeChangeARed" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-red)" /></marker>
  </defs>
  <path d="M74 120 L157 120" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#safeChangeABlue)" />
  <path d="M185 120 L268 120" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#safeChangeABlue)" />
  <path d="M296 120 L379 120" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#safeChangeABlue)" />
  <path d="M407 120 L490 120" fill="none" stroke="var(--stk-amber)" strokeWidth="1.4" markerEnd="url(#safeChangeAAmber)" />
  <path d="M518 120 L601 120" fill="none" stroke="var(--stk-amber)" strokeWidth="1.4" markerEnd="url(#safeChangeAAmber)" />
  <path d="M629 120 L712 120" fill="none" stroke="var(--stk-git)" strokeWidth="1.4" markerEnd="url(#safeChangeAGit)" />
  <path d="M740 120 L823 120" fill="none" stroke="var(--stk-git)" strokeWidth="1.4" markerEnd="url(#safeChangeAGit)" />
  <path d="M837 168 C837 262 504 262 504 172" fill="none" stroke="var(--stk-red)" strokeWidth="1.4" strokeDasharray="4 4" markerEnd="url(#safeChangeARed)" />
  <circle cx="60" cy="120" r="9" fill="var(--stk-blue)" />
  <circle cx="171" cy="120" r="9" fill="var(--stk-blue)" />
  <circle cx="282" cy="120" r="9" fill="var(--stk-blue)" />
  <circle cx="393" cy="120" r="18" fill="var(--stk-amber)" fillOpacity="0.16" />
  <circle cx="393" cy="120" r="12" fill="var(--stk-amber)" />
  <circle cx="504" cy="120" r="9" fill="var(--stk-amber)" />
  <circle cx="615" cy="120" r="9" fill="var(--stk-git)" />
  <circle cx="726" cy="120" r="9" fill="var(--stk-git)" />
  <circle cx="837" cy="120" r="15" fill="var(--stk-mint)" fillOpacity="0.16" />
  <circle cx="837" cy="120" r="9" fill="var(--stk-mint)" />
  <text x="60" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">read</text>
  <text x="60" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">CURRENT VALUE</text>
  <text x="171" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">show</text>
  <text x="171" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">TO YOU</text>
  <text x="282" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">blast radius</text>
  <text x="282" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">WHO DEPENDS</text>
  <text x="393" y="147" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">you approve</text>
  <text x="393" y="160" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">THE ONE GATE</text>
  <text x="504" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">write</text>
  <text x="504" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">TO THE GRAPH</text>
  <text x="615" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">build</text>
  <text x="615" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">REGENERATE</text>
  <text x="726" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">deploy</text>
  <text x="726" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">APPLY -K</text>
  <text x="837" y="144" fontSize="12.3" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">verify</text>
  <text x="837" y="157" fontSize="9.7" fill="var(--stk-muted)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">REAL TESTS</text>
  <text x="171" y="70" fontSize="9.7" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600" letterSpacing="1.1">PROPOSE</text>
  <text x="393" y="70" fontSize="9.7" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600" letterSpacing="1.1">DECIDE</text>
  <text x="615" y="70" fontSize="9.7" fill="var(--stk-git)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600" letterSpacing="1.1">ACT</text>
  <text x="837" y="70" fontSize="9.7" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600" letterSpacing="1.1">PROVE</text>
  <rect x="546.72" y="240.9" width="246.56" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="546.72" y="240.9" width="246.56" height="18.2" rx="9.1" fill="var(--stk-red)" fillOpacity="0.14" stroke="var(--stk-red)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="670" y="253.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">fails → return to the previous version</text>
</svg>
</Figure>

## 3 · Diagnose

A symptom is a starting point in a graph, not a search term.

The model traverses from the symptom outward: which component, which links into and out of
it, which of those links are proven and which are failing, what the logs and metrics on
either side say, and what the generated configuration actually contains. Container-level
inspection is the last resort, not the first move.

The result names the failing layer — the credential never arrived, the port is not
reachable, the role does not permit it, the target answers but rejects the credential —
rather than reporting that something is red.

## 4 · Prove

"Is my stack fine?" has an answer with evidence behind it.

| Question | What the model runs |
|---|---|
| Is it healthy? | Health map across the graph, then link validation for anything suspicious |
| Is it secure? | Scored scans: admission policy, network policy, Kubernetes hardening, vulnerabilities |
| Is it compliant? | Framework position, per-requirement detail, and the gaps with their evidence |
| Can I restore it? | What is protected, what can be proven to hold data, and which restore points are usable |
| What changed? | Deltas since the previous run, classified as explained by a declared change or as drift |

→ [Day 2 — operate and prove](/docs/day2/) · [Compliance automation](/docs/compliance/)

## Letting it run unattended

The same conversations can run on a schedule. An **agent** sub-component defines the
worker — model, allowed tool categories, budget. An **AI task** sub-component gives it a
prompt, a cron schedule, a notification target and, optionally, the next task in a chain.

Because both are sub-components of the MCP server, they are part of the graph: versioned
with the stack, duplicated into staging with it, and rolled back with it.

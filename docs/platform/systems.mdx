---
id: platform-systems
slug: /platform/systems
title: Systems and builds
sidebar_label: Systems and builds
sidebar_position: 2
description: The lifecycle of a system — save, validate, build, push — and what happens at each step.
---

<Hero eyebrow="Work" title="From a drawing to a repository">
A system moves through a small, explicit lifecycle. Every transition is recorded, and
every recorded transition carries the diagram it was made from.
</Hero>

## The lifecycle

| State | Meaning |
|---|---|
| **Created** | The system exists, with a default configuration and one environment |
| **Saved** | The current diagram is stored. Each save is its own version |
| **Building** | A build is running; logs stream while it does |
| **Generated** | The build finished and the repository was written |
| **Failed** | The build stopped; the logs say where |

<Figure caption="A build takes a saved system through building to generated or failed. Save and push skips the full build and regenerates only the files the change can affect; every save is its own version.">
<svg viewBox="0 0 900 330" role="img" aria-label="A system moves from created to saved; a build takes it through building to generated or failed; save and push skips the full build and regenerates only the files the change can affect.">
  <defs>
    <marker id="lifecycleASteel" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-steel)" /></marker>
    <marker id="lifecycleABlue" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-blue)" /></marker>
    <marker id="lifecycleAMint" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-mint)" /></marker>
    <marker id="lifecycleARed" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-red)" /></marker>
    <marker id="lifecycleAGit" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-git)" /></marker>
    <marker id="lifecycleAAmber" viewBox="0 0 10 10" refX="8.5" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M 0 1 L 9 5 L 0 9 z" fill="var(--stk-amber)" /></marker>
  </defs>
  <path d="M88 186 C169 186 169 186 250 186" fill="none" stroke="var(--stk-steel)" strokeWidth="1.4" markerEnd="url(#lifecycleASteel)" />
  <path d="M278 186 C372 186 372 186 466 186" fill="none" stroke="var(--stk-blue)" strokeWidth="1.4" markerEnd="url(#lifecycleABlue)" />
  <path d="M494 186 C612 186 612 118 730 118" fill="none" stroke="var(--stk-mint)" strokeWidth="1.4" markerEnd="url(#lifecycleAMint)" />
  <path d="M494 186 C612 186 612 262 730 262" fill="none" stroke="var(--stk-red)" strokeWidth="1.4" markerEnd="url(#lifecycleARed)" />
  <path d="M304 186 C344 36 700 36 736 104" fill="none" stroke="var(--stk-git)" strokeWidth="1.5" markerEnd="url(#lifecycleAGit)" />
  <path d="M254 198 C224 250 304 250 274 198" fill="none" stroke="var(--stk-amber)" strokeWidth="1.2" markerEnd="url(#lifecycleAAmber)" />
  <circle cx="74" cy="186" r="10" fill="var(--stk-steel)" />
  <circle cx="264" cy="186" r="16" fill="var(--stk-amber)" fillOpacity="0.16" />
  <circle cx="264" cy="186" r="10" fill="var(--stk-amber)" />
  <circle cx="480" cy="186" r="10" fill="var(--stk-blue)" />
  <circle cx="744" cy="118" r="16" fill="var(--stk-mint)" fillOpacity="0.16" />
  <circle cx="744" cy="118" r="10" fill="var(--stk-mint)" />
  <circle cx="744" cy="262" r="10" fill="var(--stk-red)" />
  <text x="74" y="211" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">created</text>
  <text x="264" y="167" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">saved</text>
  <text x="480" y="211" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">building</text>
  <text x="744" y="143" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">generated</text>
  <text x="744" y="287" fontSize="12.8" fill="var(--stk-ink)" fontFamily="Instrument Sans, ui-sans-serif, system-ui, sans-serif" textAnchor="middle" fontWeight="600">failed</text>
  <rect x="123.22" y="162.9" width="93.56" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="170" y="175.672" fontSize="10.2" fill="var(--stk-steel)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">create · save</text>
  <rect x="349.7" y="162.9" width="44.6" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="372" y="175.672" fontSize="10.2" fill="var(--stk-blue)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">build</text>
  <rect x="583.58" y="128.9" width="56.84" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="612" y="141.672" fontSize="10.2" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">success</text>
  <rect x="552.98" y="226.9" width="118.04" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="612" y="239.672" fontSize="10.2" fill="var(--stk-red)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">error · cancelled</text>
  <rect x="399.78" y="48.9" width="240.44" height="18.2" rx="9.1" fill="var(--stk-panel-2)" />
  <rect x="399.78" y="48.9" width="240.44" height="18.2" rx="9.1" fill="var(--stk-git)" fillOpacity="0.14" stroke="var(--stk-git)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="520" y="61.672" fontSize="10.2" fill="var(--stk-git)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">save &amp; push · only the affected files</text>
  <rect x="183.56" y="252.9" width="160.88" height="18.2" rx="4" fill="var(--stk-panel-2)" />
  <text x="264" y="265.672" fontSize="10.2" fill="var(--stk-amber)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle">save again = new version</text>
  <rect x="172.61" y="289.15" width="182.78" height="17.7" rx="8.85" fill="var(--stk-panel-2)" />
  <rect x="172.61" y="289.15" width="182.78" height="17.7" rx="8.85" fill="var(--stk-mint)" fillOpacity="0.14" stroke="var(--stk-mint)" strokeOpacity="0.6" strokeWidth="1" />
  <text x="264" y="301.492" fontSize="9.7" fill="var(--stk-mint)" fontFamily="Fira Code, ui-monospace, monospace" textAnchor="middle" fontWeight="600">pre-renders in the background</text>
</svg>
</Figure>

## The actions

| Action | What it does |
|---|---|
| **Save** | Stores the diagram as a new version. Optionally evaluates the catalog's suggestions against it |
| **Build** | Renders every component, aggregates the repository, runs the post-generation chain and commits |
| **Save & push** | Saves, then regenerates only what the changed attributes can affect |
| **Push** | The same incremental path, using work the platform already pre-rendered in the background after your last save |
| **Cancel** | Stops a running build between steps, and between components |
| **History** | Every version, with a cell-by-cell diff between any two |
| **Rollback** | Loads an earlier version's diagram back onto the canvas |

:::tip Pre-rendering makes pushes fast
After an effective save, the platform quietly renders the system's components in the
background. When you then push, most of the work is already done. A newer save supersedes
a running pre-render at a component boundary, so the result always matches what you last
saved.
:::

## Validation

Before a programmatic build, save-push or attribute change is accepted, the whole diagram
is checked against the catalog:

<Pills>
  <Pill>unique component and sub-component names</Pill>
  <Pill>name format</Pill>
  <Pill>catalog entry exists</Pill>
  <Pill>type matches the catalog</Pill>
  <Pill>version matches the catalog</Pill>
  <Pill>group matches</Pill>
  <Pill>exactly the catalog's attributes — none missing, none extra</Pill>
  <Pill>allowed values for select lists and toggles</Pill>
  <Pill>free text matches the catalog pattern</Pill>
  <Pill>a sub-component's parent can own it</Pill>
  <Pill>every link is one the catalog permits</Pill>
</Pills>

That is what makes "the AI cannot invent a component type" a property of the system rather
than a promise.

## Environments

A system's configuration lists its environments. The base output is shared; each
environment gets an overlay with only its differences. Adding an environment is a
configuration change, not a new design.

## The divergence list

The generated repository is yours to extend, so the platform has to know what to do when
your copy of a file differs from what it would generate.

| Choice | Behaviour |
|---|---|
| **Merge** | The default for any file without a stored choice: a three-way merge |
| **Overwrite** | The file is brought back to what the platform generates |
| **Ignore** | The platform never touches this file again |

Only *ignore* is remembered — an overwrite makes the file match, and a merge is the
default anyway, so neither needs to be stored. On a push, a conflict stops the operation
instead of committing conflict markers.

## Build logs

Logs stream while the build runs, and each step reports its own timing, so a slow build can
be attributed from the log alone. The build log ends with the exact apply commands for what
changed.

## Folders and visibility

Systems live in nested folders, personal or shared. A system can additionally be marked as
**externally visible**, which is what allows other users to pull its cells into their own
designs.

→ [Collaboration](/docs/platform/collaboration)
